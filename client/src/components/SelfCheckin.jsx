import React from 'react';

const SelfCheckin = () => (
  <div>
    <h4>
      Self check-in
    </h4>
    <div>
      Check yourself in with the keypad.
    </div>
  </div>
);

export default SelfCheckin;
